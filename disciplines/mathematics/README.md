# The Cosmic Pattern — Mathematics

*For mathematicians: topologists, algebraic geometers, number theorists, and anyone who thinks in structures.*

---

## What This Is, In Your Language

A torus knot T²(p,q) in ℝ³ is uniquely determined by two coprime integers (p,q). The question this framework asks is:

> **Which torus knot is the unique one perceivable by an observer embedded in ℝ³?**

The answer is T²(13,3) — and it falls out of a single constraint: D = 3.

The derivation is a chain of cyclotomic polynomials evaluated at D:

```
Φ₃(D)  = D² + D + 1  = 13   (R: points in PG(2,D))
r      = D            = 3    (inner winding = D by construction)
Φ₁(D)  = D − 1        = 2
Φ₂(D)  = D + 1        = 4
Φ₆(D)  = D² − D + 1  = 7
K      = D³ + D + 1  = 31   (next address in the cyclotomic chain)
A      = 4K + R       = 137  (the fine-structure integer)
```

R and r are coprime (gcd(13,3) = 1). T²(13,3) is a well-defined torus knot. Its knot invariants encode particle physics.

---

## The Fundamental Identity

```
cosh²(τ) − sinh²(τ) = 1
```

In the torus embedding:
- cosh = R / √(R² − r²) = 13/√160
- sinh = r / √(R² − r²) = 3/√160

The identity is exact at all values of τ. This is the **topological lock** — the reason time has a direction (cosh grows monotonically; there is no corresponding inverse).

The observer factor L = 1/cosh = √(R²−r²)/R = √(160/169) is the **Cartographic Perspective Factor** — the ratio of what an embedded observer can see to the total geometry.

---

## The Projective Plane PG(2,3)

The field F₃ = {0, 1, 2} gives the projective plane PG(2,3) with:
- **13 points** — one for each value of R = Φ₃(3)
- **13 lines** — dual to the 13 points
- **3 directions at infinity** — one for each value of r = 3

Total: **29 geometric elements**. Of these, r² = 9 are "inside" (accessible to the embedded observer). The remaining 20 are hidden — they constitute dark energy (Ω_Λ = 20/29).

This is not a metaphor. The bijection between the 9 observable modes and the 9 inner points of PG(2,3) is the content of CT-2 (proved in v41).

---

## The Holonomy

The 39-sector walk (R×r = 13×3 sectors) closes with a holonomy phase:

```
δ = 2π × r/R = 2π × 3/13 ≈ 83.08°
```

This is the **CP-violating phase** in particle physics. In your language: it is the total holonomy accumulated by a horizontal section of the torus bundle T²(13,3) over a complete circuit of the base space.

The CKM and PMNS unitarity matrices in particle physics are unitary **because this torus knot is closed** — the +r winding cancels the −r winding exactly over one full circuit. Unitarity is a theorem of topology, not an axiom of field theory.

---

## The Grammar Semigroup

All denominators in this framework are generated by the semigroup:

```
S = ⟨R, r, D, K, A, Φ₂, Φ₆, 2⟩
  = ⟨13, 3, 3, 31, 137, 4, 7, 2⟩
```

**Examples:**
- 39 = R·r (knot sector count)
- 59 = R·r + r²·D − Φ₆ = 39 + 27 − 7 (electroweak period)
- 439 = 3A + Φ₂·Φ₆ = 411 + 28 (first CA-layer address)
- 196 = 4·Φ₆² = 4·49 (strong coupling denominator)
- 919681 = (A·Φ₆)² = 959² (second-order reflectivity address)

Every physically meaningful denominator in the Standard Model should be expressible in S. Testing this claim is an open research program.

---

## Open Mathematical Problems

These are the open computation targets, stated as pure mathematics:

**CT-3:** Construct the full bijection from Z₁₃×Z₃ → {fermion flavors} such that the optical-depth function w: Z₁₃×Z₃ → ℤ, defined by m_flavor = v·L^w, reproduces all 12 fermion masses with denominators in S.

**CT-4:** Prove convergence and sum the Fresnel series:
```
sin(θ_C) = (r/R) · Σₙ aₙ · CAⁿ
```
where aₙ are determined by the cross-ratio structure of the projective pencil. Show the series sums to sin(13.04°) ± 0.01°.

**CT-5:** The PMNS mixing matrix is the CKM matrix observed from the "outward chart" (L ↔ β = 1/L). Construct the explicit homography between the two charts and show that it produces the measured PMNS angles under a second-order holonomy correction.

**CT-6:** Compute the induced metric on the CA-surface (the 2.5D interface, the limit δ→0 in the torus parametrization). This is the Riemannian geometry of the observer horizon.

---

## Run It

```bash
python3 ../../scripts/cosmic_pattern_ground_truth_v41.py --tier2
```

The `--tier2` flag outputs every derivation with its explicit formula and grammar address.

For the pure arithmetic scaffold, see `demonstrations/scaffold_arithmetic.py`.
For the PG(2,3) incidence structure, see `demonstrations/projective_plane.py`.
For the holonomy calculation, see `demonstrations/holonomy.py`.

---

## The Conjecture

The framework lives or dies on this single falsifiable conjecture:

> **T²(13,3) is the unique torus knot T²(p,q) with p = Φ₃(q) and q = number of spatial dimensions of the embedding space.**

In other words: the knot is not chosen. It is the knot that *names itself* in the language of the space it lives in.

If this is false, the framework collapses. If it is true, every physical constant is a topological invariant.
